import { EmbedBuilder } from 'discord.js';

export const unCuffing = new EmbedBuilder()
    .setTimestamp()
    .setDescription(`Taking off cuffs...`);

export const unCuffed = new EmbedBuilder().setTimestamp();
