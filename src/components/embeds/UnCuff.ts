import { MessageEmbed } from 'discord.js';

export const unCuffing = new MessageEmbed()
    .setTimestamp()
    .setDescription(`Taking off cuffs...`);

export const unCuffed = new MessageEmbed().setTimestamp();
