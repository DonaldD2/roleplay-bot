import { EmbedBuilder } from 'discord.js';

export const unZiptyping = new EmbedBuilder()
    .setTimestamp()
    .setDescription(`Taking off zipties...`);

export const unZiptied = new EmbedBuilder().setTimestamp();
