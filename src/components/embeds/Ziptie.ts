import { EmbedBuilder } from 'discord.js';

export const Ziptying = new EmbedBuilder()
    .setTimestamp()
    .setDescription(`Applying Zipties...`);

export const Ziptied = new EmbedBuilder().setTimestamp();
